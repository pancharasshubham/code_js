learn about javascript from basic to advance with salaar reference

console.log("Welcome to JavaScript, the Salaar way!");

- var, const, let

1) var - It's Outdated datatype which is use globle scope, var value can be accessible outside scope.
2) let - Is has block scope, it can only access in it's scope.
3) const - It has block scope like let, but once assigned, its value cannot be changed.


// function 

function attack(enemy) {
    return `${enemy} defeated!`;
}
console.log(attack("Enemy1")); // Output: Enemy1 defeated!

what is promise ? 
what is this and new object 
what is deboucing and throttling 

what is lexical scpoing 